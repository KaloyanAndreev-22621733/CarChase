spring.application.name=Car
spring.datasource.url=jdbc:postgresql://postgresql-carchase.alwaysdata.net:5432/carchase_db?useSSL=true&serverTimezone=UTC
spring.datasource.username=carchase
spring.datasource.password=Dd9987765566#
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto= update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.security.enabled=false


# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# Debug logging for Spring Security
logging.level.org.springframework.security=DEBUG

# Initialize database with test data
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true